import { ForgotPasswordStatus } from './forgot-password-status';

describe('ForgotPasswordStatus', () => {
  it('should create an instance', () => {
    expect(new ForgotPasswordStatus()).toBeTruthy();
  });
});
