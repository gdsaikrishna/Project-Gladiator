import { TrackApplicationStatus } from './track-application-status';

describe('TrackApplicationStatus', () => {
  it('should create an instance', () => {
    expect(new TrackApplicationStatus()).toBeTruthy();
  });
});
