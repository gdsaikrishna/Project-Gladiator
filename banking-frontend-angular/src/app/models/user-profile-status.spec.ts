import { UserProfileStatus } from './user-profile-status';

describe('UserProfileStatus', () => {
  it('should create an instance', () => {
    expect(new UserProfileStatus()).toBeTruthy();
  });
});
