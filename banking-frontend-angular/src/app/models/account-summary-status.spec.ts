import { AccountSummaryStatus } from './account-summary-status';

describe('AccountSummaryStatus', () => {
  it('should create an instance', () => {
    expect(new AccountSummaryStatus()).toBeTruthy();
  });
});
