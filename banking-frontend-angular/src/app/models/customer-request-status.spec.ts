import { CustomerRequestStatus } from './customer-request-status';

describe('CustomerRequestStatus', () => {
  it('should create an instance', () => {
    expect(new CustomerRequestStatus()).toBeTruthy();
  });
});
